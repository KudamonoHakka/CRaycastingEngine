#include "render.h"
#include <stdio.h>

char* load_map(const char* file_name, int width, int height);
int* map_cam_pos(char* map_area, int width, int height);

unsigned char* load_texture(const char* image_name, int width, int height);


